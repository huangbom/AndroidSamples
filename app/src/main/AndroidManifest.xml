<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wuxie">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.SampleApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mycart.MyCartActivity" />
        <activity android:name=".bieguide.BieGuideActivity" />
        <activity android:name=".bieguide.BieGuide2Activity" />
        <activity android:name=".BaoBenProductDetailActivity" />
        <activity android:name=".TextViewActivity" />
        <activity android:name=".SampleRecleViewActivity" />
        <activity android:name=".HeadLinearActivity" />
        <activity android:name=".PayPasswordEditActivity" />
        <activity android:name=".BindServiceActivity" />

        <service
            android:name=".service.SampleService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".AnkoActivity" />
        <activity android:name=".ImageActivity"></activity>
    </application>

</manifest>