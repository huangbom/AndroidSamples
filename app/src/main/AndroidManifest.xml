<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wuxie">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.SampleApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DataBindingActivity" />
        <activity android:name=".mycart.MyCartActivity" />
        <activity android:name=".bieguide.BieGuideActivity" />
        <activity android:name=".bieguide.BieGuide2Activity" />
        <activity android:name=".goldapi.RetrofitSampleActivity" />
        <activity android:name=".BaoBenProductDetailActivity" />
        <activity android:name=".TextViewActivity"></activity>
    </application>

</manifest>